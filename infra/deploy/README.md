# Deployment Infrastructure

Deployment manifests and environment contracts.

MVP note:
- Public-only workflows can start as local + single-host deployments.
- Private-repo support is out of scope until auth + isolation is designed.

