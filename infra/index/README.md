# Index Infrastructure

Indexing for retrieval:

- Phase 0: deterministic keyword search (simple and reproducible)
- Phase 1: embeddings (semantic retrieval)

This folder owns:
- index build configs
- scripts to build/update indexes
- index format contracts

