# Connectors Package

Public connectors and extractors.

Responsibilities:
- GitHub public connector (metadata + files)
- Repo signal extraction (dependencies, keywords)
- Normalization utilities (repo slug, language detection)

